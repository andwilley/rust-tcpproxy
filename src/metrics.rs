use metrics::counter;

// TODO: in progress
fn _count() {
    let _ = counter!("test");
}

struct _ProxyMetrics {}
impl _ProxyMetrics {
    // fn incr_connections();
    // fn incr_bytes_right_by();
    // fn incr_bytes_left_by();
    // fn incr_connection_failed();
    // fn incr_dns_err(error_kind);
    // fn incr_attempt_connect_while_cooling();
    // fn incr_connected_while_cooling();
}
